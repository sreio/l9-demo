<?php
namespace App\Helpers;

class HttpCode
{
    // Normal return resources, whose default value is 0.
    const SUCCESS_OK = 0;

    // The exception returns a resource with a default value of -1.
    const FAILED_ERROR = -1;
}

